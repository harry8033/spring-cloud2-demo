server:
  port: 9090
spring:
  application:
    name: eureka-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
    register-with-eureka: false
    fetch-registry: false

